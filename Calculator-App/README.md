# Calculator App
Does it really need a description?

## Required Package(s)
Python Version Used: 3.11

kivy
```bash
pip install kivy
```

## Installation Guide
Nothing complex, just install all the required packages and run the program.

## Future Plans
No future plans as it works really well.


## File Info
`calculator.kv` is same as CSS as in HTML, just the difference is that it's written in kvlang.